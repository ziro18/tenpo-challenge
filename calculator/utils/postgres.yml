# Use postgres/example user/password credentials
version: "3.1"

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: tenpo
      POSTGRES_PASSWORD: tenpo
      POSTGRES_DB: tenpo
    ports:
      - 5432:5432
